version: '3'
services:
        web:
                image: flask_no_papels
                build: .
                ports:
                        - "8765:8765"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped